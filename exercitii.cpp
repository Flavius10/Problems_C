#include<iostream>
using namespace std;

int main()
{
    int a;
    cin >> a;

    int s = 0;
    /*
    s += a; // s = s + a
    s++; // s = s + 1;

    s -= a // s = s - a;

    s *= a; // s = s * a;

    s /= a; // s = s / a;
        */

        s += a + 2 * a + 2 * a - 3;
    cout << s;
}
